# In addition to using the Netlify UI to configure build settings,
# deploy setting, and environment variables, you can also configure
# these settings here.
[build]
    publish = ".dist"
    command = "npm run build"

[build.environment]
    NPM_VERSION = "7.0.3"

[context.development]
